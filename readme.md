# Simple File Descriptor Passing

Simple demo of passing file descriptors using Unix sockets. Fairly gross
API but it works and I figured it was worth sharing.

```bash
./i_use_pid & ./i_print_pid
```
